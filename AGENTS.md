# Agent Instructions

This repo is for building an embodied Codex house companion: a small indoor robot body that can listen, talk, look around, and move safely through the house. Keep the work practical, safety-first, and a little fun. We are allowed to enjoy the tiny robot build while still treating motors, batteries, and autonomy with respect.

## Keep This File Fresh

When you learn something that would help the next agent work faster or more safely, update this `AGENTS.md` file before you finish.

Good candidates:

- Settled project decisions.
- New hardware assumptions or measured dimensions.
- CAD/export workflow changes.
- Safety constraints.
- Commands that worked or failed.
- Environment setup details.
- Gotchas that would waste the next agent's time.

Do not turn this into a full changelog. Keep it short, actionable, and project-specific.

## Current Project Direction

- MVP body: compact indoor wheeled differential-drive rover with a friendly camera head.
- Compute baseline: existing Raspberry Pi 5 8GB.
- AI HAT+ 2: not required for MVP. Reserve mechanical clearance, cooling airflow, and power budget for a future upgrade.
- Storage: start MVP with a reliable microSD card. USB 3 storage is an easy later upgrade. Official M.2/NVMe HAT storage and official AI HAT+ 2 both use the Pi 5 PCIe connector, so do not assume they can coexist.
- If USB 3 storage is added later, design a printed cradle with cable strain relief and service access. Avoid dangling external drives inside the body.
- Personality: the agent seat is pluggable — Claude or Codex is the primary conversational and agentic identity, one resident narrator at a time (D031).
- Subsystems may use other models/services for wake word, STT, TTS, perception, or fallback, but they are tools, not alternate personalities.
- Safety/reflexes are deterministic and must not depend on an LLM.
- ACCEPTED CONTROL DIRECTION (2026-07-17, D030-D032): the resident agent has full authority over the robot computer — live SSH from anywhere through an outbound-only Cloudflare Tunnel (`cloudflared` + Access service tokens, ProxyCommand mode only; D033), code and scripts written on the fly, package/service installs, and direct velocity/head setpoints through the `robotd` body daemon, all captured in a blackbox plus recorded shell sessions streamed to an off-host mirror (Pi-local copies are best-effort against root). Every hard safety guarantee (velocity/accel clamps, motion-setpoint leases — a nonzero setpoint zeros in firmware unless refreshed, watchdog, latched NC-bumper/E-stop/charger stops, low-battery cutoff, hardware mic mute in the USB VBUS conductor with the backfeed gate) lives in safety-MCU firmware and physical controls the Pi cannot reach: the Pi-to-Pico UART protocol has no flash/config-write path, and the Pico's USB/SWD are service corridors never cabled to the Pi in operation. The fixed intent vocabulary is retired as a boundary and survives as the starter behavior library; no-go zones/quiet hours/supervision are audited policy, not enforcement. See `docs/agentic-control-plan.md`.
- Build order: documentation -> bench brain -> safety loop -> CAD fit checks -> rolling chassis -> supervised autonomy.
- Purchased-part sourcing: every production hardware item must be available in quantity one through a normal US online checkout from a mainstream retailer or distributor. Printed parts and assembled wiring harnesses are expected; RFQ-only parts, overseas-only sources, factory minimums, custom-machined metal, custom sheet metal, and custom-fabricated PCBs are not acceptable unless the user explicitly approves an exception. Prefer two US sources for safety- and power-critical parts.
- RELEASE STATUS (2026-07-17): the tracked v2 3MF and builder's book are prototype geometry/dry-assembly previews only — never describe them as a complete retail kit or use them to authorize powered motion. The D035 head/neck load paths are CAD-complete, and the repo now ships a bench-safe Pi installer/`robotd`, fixed framed-UART contract, host-tested portable Pico safety core, deliberately fail-stopped Pico target, nominal 28-route harness traveler, and executable 27-step commissioning gate. Exact-part fit, loaded journal/bushing wear and servo-current tests, production motor/encoder/head outputs, exact terminals/measured harness lengths, released fuse values, and signed physical evidence must exist before that label changes. Page topology must always be Pi/`robotd` -> Pico over framed UART -> MDDS10; Pico USB/SWD are service-only and never cabled to the Pi in operation.
- SAFETY CONTRACT (2026-07-18, D036): any open SPST-NC bumper loop means zero motion. A pressed switch and broken/unplugged wire are electrically indistinguishable, so capped escape while open is retired. Clear only after all requested loops read closed; never add a time- or direction-based exception without independently supervised position evidence.
- BENCH CONTROL COMMAND (2026-07-17): run `python3 commissioning/run_host_tests.py` from the repo root. It creates a temporary, network-independent Python 3.11/3.12 environment, tests `robotd` protocol/socket/simulator behavior, validates the engineering harness schedule and its deliberate physical-release hold, and compiles/runs the portable C safety core with CMake/CTest or a strict C11 compiler fallback. GitHub runs the same entry point; [`docs/ci-safety-gates.md`](docs/ci-safety-gates.md) is the authoritative statement of what green proves. Passing is hardware-free evidence only; `harness/generate_harness_docs.py --release` and commissioning verification must stay red until exact first-article evidence is recorded.
- FIRST-ARTICLE EVIDENCE (2026-07-17): `commissioning/commission.py` schema v2 creates a private per-robot bundle pinned to a clean immutable release tag, imports declared step artifacts into a SHA-256 object store, preserves hash-chained event revisions, and seals only a complete C001-C027 record with OpenSSH. Independent verification always requires an out-of-bundle `allowed_signers` policy, expected signer, expected robot serial, and source clone. A valid signature means the signer attests to exact bytes; it does not prove measurements true or authorize motion. Never commit `output/commissioning/`; see `docs/first-article-evidence.md`.
- BENCH FIXTURE COMMAND (2026-07-17): `python3 commissioning/fixture.py --check` validates the closed-world `RB-FIXTURE-V1` circuit against the live Pico pin definitions and checks its generated guide, paper labels, and OpenSCAD data. The support-free PETG jig is 232 x 210 x 79 mm overall with a dedicated headered SC1632 Pico, six real NC switches, real IDEC E-stop/relay, and no battery, motors, or MDDS10. Its current 12 V stage is limited to 0.20 A and is negative-control only: GP11, coil, and lamp must remain off. No physical pass is bundled.
- BENCH DASHBOARD (2026-07-19, D037): `software/dashboard/` is the stdlib-only localhost supervision dashboard — an HTTP/SSE client of the `robotd` socket with hold-to-drive controls, decoded safety flags, bumper-zone view, and a live blackbox tail. Run `robotd --simulate` plus `robot-dashboard` (see its README) and open `http://127.0.0.1:8072/`. It refuses non-loopback binds without `--expose-lan`, adds no permission gate, and its camera/agent-panel/settings cards are deliberate "not built yet" placeholders. The robotd blackbox is a state-change journal: read-only `status` queries and bare uptime ticks are not journaled; every state-affecting command still is. AF_UNIX socket paths must stay short (macOS ~104 bytes) — deep worktree scratch dirs are too long for test sockets. The book's dashboard picture is a real capture: `python3 scripts/capture_dashboard_screenshot.py` boots the simulator + dashboard, replays the narrated `--source agent` session, screenshots with headless Chrome into `docs/images/guide_v2/dashboard_bench.png`, and verifies the blackbox actually contains that session. Rerun it after any dashboard UI change, then regenerate the guide; it refuses to run if port 8072 is already occupied.
- v2 GUIDE CHAPTER 5 (2026-07-19, release v2.0.0-prototype.4): the book's "Check & play" chapter is now the bring-up path, not just the gate list — page 42 runs the shipped `robotd --simulate` + dashboard on any Mac/Linux with Python 3.11+ (real captured screenshot, see D037 bullet), page 43 condenses the Pi-appliance install (full recipe stays `docs/pi-appliance-provisioning.md`), and page 44 seats the resident copilot: full-Pi-authority story, the firmware/physical floor it cannot cross, the journaled `--source agent` robotctl session a builder can replay. Keep those pages consistent with the software READMEs and the agentic-control-plan; the book still never authorizes powered motion, and `robotd` still ships parked (stopped/disabled) on first install.
- PI APPLIANCE COMMAND (2026-07-17): `software/install.sh` provisions only an aarch64 Pi 5 on Bookworm from the closed-world `software/appliance/appliance-v1.json` contract. It requires an option-free Ed25519 public key, a Tunnel run token, a yescrypt console-password hash, and explicit Access-ready attestation. It preserves Pi 5 debug UART10, maps RP1 GPIO UART0 (`/rp1/serial@30000`) to `/dev/rover-pico`, leaves first-install `robotd` stopped/disabled, and never reboots. The RW audit mount is backup only; keep the live gate red until a separate append-only collector denies the Pi credential read/delete/alter and an independent machine reads back the nonce. See `docs/pi-appliance-provisioning.md`.
- ACCEPTED MATERIAL DIRECTION (2026-07-17, D034): the v2 registry keeps material family, mechanical role, and theme color separate. Direct PLA = fascia, rear panel, head faceplate, two diffuser bars, and optional lid skin. Shell/head request body-color PLA but automatically output as white PETG until the exact product line passes its recorded insert/impact/thermal/fit/motion gates. Fixed PETG is white structure, black wear/motion, or red service retention only; bumper halves, tires, and battery pad remain charcoal TPU 95A. The complete white PETG lid is the only E-stop/head/mic load path; its teal PLA skin is optional cosmetic inventory with blind-pocket tabs. Current count is 40 functional + 4 spare washers + 1 optional = 45 printed pieces on 13 plates; 18 qualification proofs, zero support exceptions. Keep this prototype indoor-only and never claim a polymer is fire protection.
- BUILD-KINDNESS + SITE DIRECTION (2026-08-12, D040-D047): the guide now ships as TWO renderers over one content source — `docs/generate_assembly_guide_v2.py` (book/PDF) and `docs/generate_guide_site_v2.py` (interactive offline-first site → `output/site/`, gitignored, built by cad-release CI); the site imports the book module, so never hand-fork guide content, and `docs/guide_estimates_v2.py` is the single source of filament-mass/print-time planning estimates for both. Every body plate carries a corner QC proof tab (D040, `BAMBU_V2_QC_TABS_VALID qc_tabs=13`; tabs are print-layer QC pieces, never registry parts). The safety core self-reports stop latency over EVENT 0x82 (D041; read-only, no config path, probe cross-check still required); `robotd` journals them. Servo horns land on the nearest spline tooth with embossed index marks + `robotd --head-trim-*-cdeg` Pi-side trim (D042; ±800 cdeg clamp), and `robot-hello` is the narrated mid-build wake-up milestone. D044-D046 are PROPOSED directions for the protected Pi input, Pico reset, and mic VBUS-cut blockers — do not buy or wire from them. D047 is the zero-skill harness termination policy (pre-crimped > lever nuts > one named crimper > solder-only-where-required); the harness release gate stays red.
- AGES CHIP INTENT (2026-08-12, D039): the book's "AGES 10+ WITH AN ADULT" cover chip and D029's child-followable rule are a **plain-language bar** for everything the builder reads — simple enough for a ten-year-old working with an adult, no jargon, no engineering degree assumed. They are NOT a consumer age grading, toy-safety classification, or product safety promise; a real retail age label is a separate future compliance decision. Keep the chip and the simple wording, never cite it as a safety claim, and never "fix" the bar-vs-tool-list tension (soldering iron, multimeter, servo tester) by hardening the language — it resolves by simplifying the build experience. See `docs/decision-log.md` D039.
- TERMINOLOGY (2026-07-19, D038): the v2 qualification pieces are called **proofs**, never "coupons" — each one proves one design contract before large parts print. Renamed across v2 code, JSON schema fields, tracked manifests, file names (`robot_body_v2_proofs.py`, `generate_bambu_proofs_v2.py`, `codex_robot_body_v2_proofs_p1s.3mf` + plates JSON + contact PNG, `docs/cad-proofs.md`), CI tokens (`CAD_RELEASE_PASS ... proofs=...`, `BAMBU_V2_PROOFS_VALID`), and the current book. Historical v1 material and dated records (decision log, `printed-only-simplification-plan.md`, `guide-v2-improvement-notes.md`, the frozen v1 block below) keep their original "coupon" wording. In prose, prefer "qualification proof" where bare "proof" could read as generic evidence.
- ACCEPTED v2 HEAD DIRECTION (2026-07-17, D035; drive interfaces superseded 2026-08-12, D048): three-lug fixed collar in the PETG lid; 39.6 mm neck journal in a 40.3 mm greased printed bore with flat thrust shoulder; D85MG captured by the collar cradle + two-M3 black plate; four-M3 yoke; 3.2 mm printed passive shoulder bushing clamped by one M3 x 8; +/-60 pan and +/-20 tilt command ranges with printed stops beyond. D048 retired the R-ML24 aluminum horn and all M2 link screws: both drive interfaces are printed ARM-CAPTURE CRADLES that seat the generic single arm from the D85MG's own bag (torque on the slot walls, pan cover clamped by the standard M3 system, arm-envelope constants PROVISIONAL pending delivered-arm measurement, embossed centering ticks at pan/tilt center, residual offset absorbed by `robotd --head-trim-*`). M3 is now the only fastener thread in the robot. `validate_robot_body_v2.py --gate` samples the actual moving solids, checks servo/cable clearance, and proves stop engagement. The old 6807/MF84ZZ/shoulder-screw stack is v1 history, not a v2 purchase.
- v2 GUIDE/RENDER REVIEW PASS (2026-07-17, see `docs/guide-v2-improvement-notes.md` for the full audit): the robot faces -X, so robot-RIGHT is +Y — several book directions had been mirrored (fuse block left half, MDDS10 terminals right, regulators under the deck's rear half with 6 V wire exiting front, Pico USB toward the back); keep new text consistent with the registered envelopes, not the renders. Model fixes landed: the lime eye bar is now a 98 mm goalpost (plates behind each eye slot bridged ABOVE the camera aperture — a straight bar would block the lens); every tire carries one 7.2 mm radial tread port so the rear wheels' rim clamp screw stays reachable with the tire mounted (align port over screw hole; melt the rim insert before the tire goes on); the rear wheel's clamp bore is a STEPPED WELL (7.4 head channel from the rim, 4.6 insert seat deep in the wheel, tip clearance ending 0.3 mm past the shaft flat) because a rim-surface insert left the single-SKU M3 x 8 tip 12 mm short of the shaft; and the printed washer is a 22 OD x 3.2 front-axle CAP washer because the old 8 mm washer fit entirely inside the front wheel's 18.5 mm bore and retained nothing (3.2 face also satisfies the D026 clamp stack). Always sanity-check that a screw's reach and a retainer's overlap actually close the load path before trusting a joint entry. The step renderer (`render_assembly_steps_v2.py`) exports normalized camera-projected part/destination coordinates; the guide draws crisp numbered magenta 2D arrows and dimensioned sections for hidden interfaces. It also renders gold insert markers, the 2x2 wheel bench, and dedicated close panels for Pico, deck regulators/fuse, all six switches, rear panel, audio, full E-stop stack, pan stack, tilt stack, and camera. Heroes retain the dark camera lens; the electronics page/back use the populated cutaway, not an empty chassis. Fastener proxies never join `placed`. White PETG receives a temporary cool-grey instructional tint and stronger rim light only in renders. The guide CSS crops step images at 107%; plate-table names match the piece chart; dark plate thumbnails gamma-lift. `generate_bambu_proofs_v2.py` creates the tracked 18-test/19-object/five-plate proof project; the PETG tire core and TPU ring must remain separate. The PDF generator deletes only its exact old target before Chrome and verifies the emitted page count with pypdfium2; PDF existence alone is not a valid regeneration check.
- ACCEPTED v2 DIRECTION (2026-07-16, D025-D027): the body will be reworked to printed-only structure — no purchased metal brackets, standoffs, spacers, shoulder bolts, bearings, or hubs; the only separately purchased body fasteners become one insert SKU + one screw SKU (M3 x 5.7 heat-set inserts + M3 x 8 SHCS, bundled component hardware exempt); the footprint targets a 238 x 220 x 133 one-piece shell/tray/lid (height computed on verified datums: MDDS10 thermal ceiling 86.275 + Pi clearance + full 96 x 74 x 22 AI-HAT reserve + 3 mm to the pan-servo drop at 131.6). See `docs/printed-only-simplification-plan.md` and decision-log D025-D027. The box screen (`cad/python/packing_study_printed_only.py`, layout v5.1 after four review rounds) closes with a 2 mm margin policy including wall margins; the inventory-driven validator enforces the registered envelopes, <=40 installed functional parts, one screw/insert system, per-family mass, and zero-support audit. The current v2 chain is `robot_body_v2_inventory.py` -> `robot_body_v2.py` (32 solids) -> `validate_robot_body_v2.py --gate` -> `robot_body_v2_print.py` + `robot_body_v2_proofs.py` (18 logical proofs) -> `generate_bambu_project_v2.py` (45 total pieces, 13 plates, seven material/color groups) + `generate_bambu_proofs_v2.py` (19 objects, five plates) -> both Blender renderers -> `generate_assembly_guide_v2.py`. The book stays child-followable WITHOUT any "grown-up needed" callouts (D029) and must be verified page-by-page with pypdfium2. Run the validator after ANY v2 change. The v1 conventions and regen instructions below remain authoritative for v1 only; do not mix the two.

## Read These First

Start with:

1. `README.md`
2. `docs/README.md`
3. `docs/decision-log.md`
4. `docs/mvp-prd.md`
5. `docs/mvp-architecture.md`
6. `docs/agentic-control-plan.md`
7. `docs/builder-release-v2.md` (current purchase authority; `bom-v0.md` is v1 history)
8. `docs/cad-mechanical-plan.md`
9. `output/pdf/codex_robot_body_v2_assembly_guide.pdf` (the current builder's book; the v1 guide is historical)

The docs intentionally separate product requirements, decisions, architecture, BOM, and CAD/mechanical planning. Keep new details in the most specific doc rather than stuffing everything into the README.

## Safety Rules

- The robot should fail stopped.
- Physical E-stop must cut motor power independently of the Pi.
- Bumper switches and watchdog timeout must stop motion without consulting the agent.
- The resident agent (Claude or Codex) has full authority over the Pi (D030): shell, sudo, installs, code on the fly, and direct velocity/head setpoints through `robotd`, all blackbox-logged.
- The agent cannot exceed firmware velocity/accel clamps, clear an E-stop latch, bypass the watchdog or NC bumper loops, restart motion past the charger inhibit, or un-mute the hardware microphone switch (D032). Never give the safety MCU a remote flash or config-write path, and never cable its USB/SWD to the Pi in normal operation.
- No-go zones, quiet hours, and supervision rules are policy the agent is instructed to honor and the blackbox audits — do not describe them as hard enforcement.
- Do not add software permission gates between the agent and its actions (Brian's direction, 2026-07-17). The floor exists for malfunction handling (setpoint leases, watchdog), hardware protection (clamps, fusing, low-battery cutoff, wiring-fault latches), and the humans' physical overrides (E-stop, mute) — never to constrain what the agent chooses to do. Per D036, any open bumper loop means zero motion; latches clear only after the requested loops read released.
- Local `stop`, `wait`, `mute`, and basic `status` should work even if network/cloud AI is unavailable.

If you touch any motion, power, battery, or safety-control design, update the relevant docs and make the safety implications explicit.

## CAD And Mechanical Conventions

**V2 routing:** use `cad/python/README.md`, the v2 inventory/model/validator,
the v2 Bambu projects, and the 45-page v2 book. The long implementation notes
below are frozen v1 conventions unless a bullet explicitly says v2; words such
as “current” inside that historical block mean current only for a v1 rebuild.

- OpenSCAD is the default first-pass CAD tool for simple parametric printable parts.
- Local OpenSCAD CLI is available at `/Applications/OpenSCAD-2021.01.app/Contents/MacOS/openscad`.
- `build123d` and CadQuery are approved for Python CAD when geometry needs richer fillets, chamfers, STEP exports, or complex assemblies.
- The authoritative visual reference currently present in the repo is `docs/images/concept-rendering.png`; do not rely on older handoff references to absent `concept_body.py` or `concept_body_blender.py` files.
- The current printable body source is `cad/python/robot_body.py`, a build123d model that exports modular STL/STEP parts and review-only electronics envelopes under `cad/exports/`.
- The molded body shell uses a 12 mm top-edge-only exterior rollover and a matching 8.8 mm cavity rollover, preserving the full 3.2 mm roof while keeping the lower tray and seam-plate interface crisp. The validator requires at least 10 mm of visible rollover and probes roof material at three quiet locations; do not return to filleting every shell edge or restore the old cavity overcut that reduced the roof to 2.2 mm.
- `cad/python/robot_body_split.py` generates 220 mm-bed-friendly shell, tray, lid, reveal/gasket, bumper, fairing, insert-backed seam-plate, and under-deck harness variants. The current 57-part split contract is four shell quadrants plus four wall plates, two tray halves plus one underside plate, four bumper quadrants plus four low plates, four independently mounted fairing halves, a two-half 12 mm lap lid, and two small harness rails. The nine backing plates carry 18 concealed 4 mm self-locating pilots with 0.3 mm radial and 0.4 mm axial recess clearance. Regenerate it after changing dimensions instead of hand-editing exports.
- `cad/python/robot_body_print.py` is the canonical slicer export. It replaces seven oversized main parts with split variants, deliberately orients/grounds 101 prototype STLs, verifies volume/bed/contact invariants, and writes material, support, hardware-release, and head-static-torque guidance under `cad/exports/print_ready/`. Run it after the main validator; do not treat "print-ready" as hardware-release-ready.
- The settled printer target is a Bambu Lab P1S with the standard 0.4 mm nozzle. Use the 256 mm square bed, an 8 mm layout edge reserve, and Textured PEI Plate in the project. `cad/bambu/generate_bambu_project.py` builds and Bambu-round-trips the tracked 101-part, 26-plate `cad/bambu/codex_robot_body_v1_p1s.3mf`; plates are separated into 14 material-profile/color groups, and `codex_robot_body_v1_p1s_plates.json` is the exact plate/recipe inventory. The generator derives its exact-object checks from the print manifest rather than a hard-coded count. Regenerate it after the canonical print STLs; never hand-edit the 3MF archive.
- `docs/generate_assembly_guide.py` builds the 20-page landscape Letter guide at `output/pdf/codex_robot_body_v1_assembly_guide.pdf` from the current manifests and renders. Regenerate it after any part, hardware, assembly-order, release-gate, plate, or illustration change; render every page with `pdftoppm` and visually inspect it before delivery.
- `cad/python/requirements-v1.txt` adds ReportLab for the historical v1 assembly-guide generator. Install it only for a v1 rebuild, then run `.venv-cad/bin/python docs/generate_assembly_guide.py`; do not substitute a user-specific cache-runtime path for this portable project command.
- The rear I/O interface is a 126 x 58 mm four-M3 frame with three flush 30 x 30 mm cartridges, each using a 26 x 18 mm tongue plus two M2.5 insert-backed frame screws. From the rear, left carries a Switchcraft EN2P3M20 sealed three-contact charge-only inlet with EN2C3F20G2 cord mate; center is blank with no jack, PCB, or wiring; right carries an E-Switch PVB3F230SS311 maintained SPDT physical-mute switch through its exact 16.0 mm / 14.6 mm-flat cutout. The EN2 uses two contacts for the isolated 14.6 V charge pair and one for CHARGER_PRESENT; it exposes no battery output and must be mated only with charger AC removed. The mute switch physically routes fused microphone 5 V either to microphone VBUS or to the red-ring mute indication/protected state-input branch. Internal service requires shutdown or physical motor-branch isolation.
- `cad/python/robot_body_coupons.py` generates seventeen shared-parameter calibration parts, including a lightweight production-derived Blue Sea 5045 envelope gauge with both M4 paths. Continue using the insert, clearance, 608/6807, wall, lid-lap, split-pilot, bumper-interface, flush-fairing, and retail-block coupons, and print each with its production material/profile before updating `Params`.
- The retail-only tray has no carry handles or custom clamp plates. Lift only the unpowered robot with two hands under the tray; never lift it by the shell, lid, head, bumper, fairings, wiring, or rear cartridges.
- The battery prototype baseline is the Bioenno BLF-1203AB 12 V/3 Ah LiFePO4 pack, rotated to a 110 x 75 x 27 mm envelope. Bioenno documents 7 A continuous, 14 A for two seconds, internal PCM/BMS protection, a Powerpole discharge lead, separate 14.6 V charge lead, MSDS, and UN38.3 transport evidence. The 117 x 92 mm cradle uses two 20 mm real straps, positive side/end locators, four support pads, four M3 tray anchors, and a rear lead corridor. This is not household-motion release: require delivered-part/lead fit, product-safety evidence beyond UN38.3, nonconductive padding, measured sustained pack current <=5.6 A, 45-minute runtime with 20% reserve, BMS/regen/thermal tests, and loaded retention tests.
- The E-stop baseline is the IDEC XW1E-BV402M-R: 40 mm red mushroom, 2NC direct-opening contacts, 37 mm rear body, and 48.7 mm depth. It clamps only through a removable 4 mm keyed top panel; the lid, shell, raised backing collar, and notched power deck provide a 40 mm service passage. Four cardinal M3 screws on a 28 mm radius tie the backing to blind inserts in the top panel, while the purchased nut remains primary switch retention. Add a durable 60 mm yellow legend and keep release blocked on purchased-part fit, push/twist, dual-NC, terminal-access, and independent motor-cut tests.
- Two 108 x 14 mm U-channel harness rails with 7.6 x 5 mm validated bundle corridors mount under the power deck through four paired reusable-strap stations each. Reserve the left rail for signal/audio/sensors and the right rail for fused switched power/motors. The rails deliberately clear the battery, motor covers, mobility pods, deck standoffs, and AI-HAT envelope; print one and load it with measured bundles/connectors before releasing either rail.
- The upper power deck is 145 x 116 mm. The Pi rail targets a Pololu D24V90F5 on four 6 mm M2 metal standoffs; the separate 6 V head-servo rail targets a Pololu D36V50F6 on three. The motor cutoff is the Panasonic CB1A-R-M-12V sealed SPST-NO automotive relay with integral bracket, one 5.4 mm mounting hole, 40 A contacts at 14 V, 12 V/134 mA coil, built-in resistor, and 6.3 mm quick-connect terminals. Keep the E-stop's two direct-opening NC channels in the low-current fail-deenergized coil-enable path. Purchased fit, terminal/conductor sizing, strain relief, driver/suppression review, dropout, thermal, contact-fault, and independent cutoff tests remain release gates.
- Accessory distribution is the complete Blue Sea Systems 5045 covered four-circuit ATO/ATC fuse block, 92.5 x 43.8 x 32.5 mm with two mounting holes on 65.1 mm centers. It is oriented lengthwise across the deck; lift the removable, unpowered deck for cover and fuse service. Preserve the conservative 6 A total / 5 A any-branch ceilings, battery-near feeder fuse, covered live parts, measured-load fuse selection, strain relief, labels, selective-fault tests, all-branch thermal soak, and separation from the independently relay-cut motor branch. D020's Littelfuse 880024 rejection and D023's custom Nano2 board remain history, not current sourcing.
- The shared mobility stance is wheel radius 43 mm, axle Z=66, tire ground Z=23, wheel centers Y=+/-118, and 4 mm tire inset into the 220 mm body envelope. Each TPU tire carries 24 offset transverse grooves, 5.5 mm wide x 2 mm deep, across its central 20 mm while both 2 mm sidewall bands and the floor tangent remain intact. The bumper bottom is Z=29 for 6 mm static clearance; concealed low inboard bridges keep it one solid after the wheel cuts. All four 42 mm-high pod shells still seat at Z=53. Keep wheel wells, tread, fairings, bumper reliefs, tray pads, pod hardware, Blender floor, and `codex_robot_body_v1_wheel_detail.png` derived from these shared parameters.
- Each side fairing is a 2 mm flush scalloped skin seated in a 2.2 mm shell recess with 0.3 mm perimeter clearance, a 0.2 mm nominal clamp gap, and 1 mm of continuous shell backing. A 12 mm upper bridge joins the two wheel-arch caps; the center relief stays open around the side ToF and lower shell seam. Four high M3 screws retain each one-piece side, while the 220 mm split creates four independently mounted halves with two high screws each. Keep the exterior at Y +/-110, print the exact two-piece fairing coupon before the large skins, and do not reintroduce a proud/full-height side slab or claim the lower shell-seam screws retain the fairings.
- The top vent is an 80 x 62 x 1.6 mm dark PETG inlay seated in a 1.4 mm recess, not a floating hood. Nine 36 x 3 mm slots align through inlay, lid, and shell. Four M2.5 screws at X=10/82, Y=+/-27 enter lid-side underside bosses and bridge the stepped lid split; the microphone cradle uses X=40+/-20, Y=+/-40 roof bosses, and the cable strain plate is at Y=-55. Preserve those shared clearances and the dedicated `codex_robot_body_v1_top_detail.png` review.
- Blender 5.1 is available at `/Applications/Blender.app/Contents/MacOS/Blender`; `cad/blender/render_robot_body.py` renders the actual generated STL inventory rather than maintaining a second concept mesh. Its concept-matched review lighting uses a warm window key, warm room/front/rim/overhead bounce, a low-energy soft sun, cream world fill, honey floor, 6200 K white balance, AgX Very High Contrast at +1.85 stops, 128 samples, and 32 mm ambient-occlusion reach; preserve that high-key daylight unless a visually verified improvement replaces it. Keep matching `docs/images/concept-rendering.png`: molded cream appliance shell, flush darker-teal lid, thin reveal, rear E-stop, flush dark slotted vent inlay, recessed black front sensor pill with lime lights, continuous charcoal bumper, inboard wheels, rounded cream head with an inset black faceplate, and a compact lens stack. Avoid reintroducing a proud black head panel, raised vent hood, flat planar front, detached bumper rails, giant slab fenders, oversized wheels, or merging the sensor fascia into the rubber bumper.
- The Blender concept should model the body parts only. Do not add visible internal electronics to it; use empty service-bay pockets, bosses, rails, alignment pegs, and review-only safety envelopes instead.
- Assembled and exploded Blender views should use the same part inventory. The exploded view should move part groups apart, not invent special geometry that cannot be put back together into the assembled view.
- `docs/generate_assembly_guide.py` flattens render assets to RGB before PDF embedding; keep the exploded head-mechanism image on its current high-quality JPEG path and do not reuse it on the following head-detail page. After guide changes, render all 20 pages with Poppler and inspect pages 14-16 directly. The Codex image viewer can falsely display black bands on a valid RGB QA PNG; check actual edge pixels or convert the QA copy to JPEG before diagnosing PDF corruption.
- Keep editable CAD source in `cad/openscad/`, `cad/python/`, or `cad/blender/`.
- Keep generated exports under `cad/exports/`; generated STL/STEP/mesh files are ignored by Git by default.
- Use shared parameters for dimensions, fastener sizes, wheel geometry, sensor offsets, keepouts, and board mount patterns.
- The build123d v1 source uses Raspberry Pi 5's published 85 x 56 mm board reference, Camera Module 3 Wide's published 25 x 24 x 12.4 mm envelope, Cytron's official MDDS10 STEP geometry, Pololu's D24V90F5/D36V50F6 regulator drawings, the Panasonic CB1A-R-M-12V relay envelope, Blue Sea Systems 5045 dimensions, WDS 615-M6-8-65 shoulder-bolt geometry, stock goBILDA spacer/shim dimensions, Adafruit's official 5975 NeoPixel breakout STEP, the official #3006 MAX98357A PCB/two-hole pattern, E-Switch's PVB3F230SS311 mute-switch drawing, Switchcraft's EN2 series drawings, and Bioenno's BLF-1203AB manual.
- Keep the shell/tray fastening contract synchronized: four shell insert lugs and tray clearance holes at X +/-124, Y +/-84 mm, plus the tray locating lip. Moving only one side will make assembly impossible in a very on-brand tiny-robot way.
- The current audio/sensor reference mounts use a 70 mm ReSpeaker ring, 70 x 30 x 17 mm enclosed speakers with a 64 x 24 mm mounting rectangle, two Adafruit #3006 MAX98357A mono amps as a stereo pair, and 25.5 x 17.5 x 4.6 mm VL53L1X boards. Each amp mounts component-side-down beneath its speaker plate through the official two 2.5 mm holes on 12.7 mm spacing and two 6 mm M2 standoffs. The official 2022 STEP omits the terminal block now supplied pre-soldered; require one current-board measurement of terminal height, screwdriver travel, wire bend, and header/direct-wire stack before releasing both plates.
- Audio supports must keep their shared roof-boss fasteners: four M3 paths for the microphone cradle and two M3 shell paths per speaker plate, plus two M2 amp paths per plate. Front ToF pods screw to the removable fascia; side ToF pods and windows are centered at X=-20 mm, screw to shell bosses at X=-40 mm, and require open paths through both shell and fairing.
- The v1 interior layout must remain collision-free: Pi center (-65, -35), controller center (-65, 33.5), safety MCU center (-65, 33.5) on a shelf at Z=90, and BLF-1203AB center (60.9, 0), all in millimetres. The two battery-side shelf posts sit at Y=41 and 66 mm; re-run the pairwise fit-volume audit after moving any of them.
- Run `.venv-cad/bin/python cad/python/validate_robot_body.py --bed 256 --margin 8` after any CAD dimension or layout change. The validator contract includes the retail Panasonic relay bracket/body/terminal service, Blue Sea 5045 body/cover/two-hole mounting/fuse and wire service, WDS shoulder-bolt/washer/locknut/stock-goBILDA-spacer idler stack, blank center cartridge, and absence of carry hardware in addition to the shell, bumper, battery, drivetrain, controller, sensor, head, and split-joinery checks. A passing validator is necessary but does not replace purchased-part fit, electrical review, or physical safety tests.
- Run `robot_body.py`, then `robot_body_split.py`, `validate_robot_body.py`, `robot_body_print.py`, and `robot_body_coupons.py`. The main export clears generated assembly STL files; the split, print, and coupon manifests are the authoritative current assembly, slicer, and calibration inventories and prevent stale geometry from masquerading as current output.
- The bumper is a hollow TPU perimeter carrier with a 302 x 222 mm inner cavity, giving the 300 x 220 mm shell 1 mm nominal radial clearance and the tray 5 mm. Two concealed below-tray inboard bridges keep the wheel-cut carrier connected without occupying rigid chassis volume. Six Omron D2HW-C202MR sealed SPST-NC pin-plunger switches mount on separate 22 x 40 x 4 PETG plates using their drawing-backed 13 mm M3 pattern and side-lead corridor. Keep the TPU mechanically independent of the plate screws. The shared 0.4 mm rest gap, 2 mm nominal stroke, and paired 2.4 mm rigid PETG overtravel stops must pass exact coupon fit, no-preload, actuation, rebound, strain-relief, broken-wire, six-zone electrical, and deterministic motor-cut tests before powered motion.
- The build123d head is a compact 72 D x 140 W x 72 H structural capsule centered at Z=242, with approximately 84 mm overall depth including its projecting bezel and rear cover. It uses a removable underside pan plate, 53 mm shell/lid opening, keyed fixed 6807 outer-ring carrier, 34.8 mm rotating neck journal, 37.5 mm upper shoulder, four-screw lower retainer, hollow hourglass neck with a flat ribbon corridor, and four blind M2.5 inserts in its top flange. Pan and tilt use drawing-backed Hitec D85MG 24T servos and R-ML24 horns. The matching fixed yoke carries a three-screw active adapter plus an MF84ZZ passive bearing and McMaster 92981A143 shoulder screw. The rear cover carries an interior-only yoke sweep relief buffered to +/-21 degrees while its exterior stays clean. The print manifest's 100%-dense PETG plus 25 g payload estimate is about 0.598 kg-cm static tilt torque versus the D85MG's published 0.9 kg-cm peak-efficiency torque at 6 V; this is only a sanity check. The validator rotates the complete moving printed head through +/-20 degrees tilt sampled across +/-60 degrees pan. Print the 6807 coupon and require purchased-part fit, measured mass, regulated-power/current, cable-drag, backlash, heat, and repeated-motion tests; keep `codex_robot_body_v1_head_mechanism.png` synchronized.
- The printable head's integrated cream bezel projects to X=-69.5 mm, while the 120 x 56 x 3 mm black faceplate is recessed 3 mm behind it with 1 mm perimeter clearance. The faceplate now includes its own fused stepped camera annulus, so do not recreate loose overlapping lens rings or any printed disk across the optical path. Keep the 9 x 14 mm vertical lime eye diffusers and eyebrows inside the bezel, preserve the 13 mm front clear radius, and use the shared `head_face_mount_positions()` / `head_rear_mount_positions()` helpers so all eight panel screw paths remain validated.
- The Camera Module 3 Wide carrier follows Raspberry Pi drawing RP-008155-DS-1: 25 x 23.862 PCB, four 2.2 mm holes on 21 x 12.5 mm spacing. Keep the PCB/lens fit as separate fused shapes so the lens uses the optical aperture without treating PCB corners as lens geometry. Its lens face is positioned 1 mm behind the faceplate front, and the validator treats the published 102-degree horizontal view as a conservative circular cone; release still requires a real full-resolution corner, focus, and reflection test.
- The upper 145 x 116 power service deck carries drawing-backed Pi and servo regulators, the bracketed Panasonic relay, and the covered Blue Sea 5045 fuse block above the battery on four metal standoffs. The two MAX98357A boards live beneath the speaker plates instead; do not freeze an I2C mux unless bench wiring proves the ToF XSHUT/address baseline insufficient. The rear 126 x 58 frame has charge-only, blank-center, and physical-mute cartridges.
- Power-deck standoff envelopes must run continuously from the tray top at Z=49 to the deck bottom at Z=102. The top cable strain-relief plate uses two M3 roof bosses in addition to its six tie points; neither interface may float between layers.
- The rear drivetrain baseline is two Pololu #4867 99:1 25D MP 12 V encoder gearmotors, #1569 metal brackets, and #1997 4 mm-shaft M3 aluminum hubs. The #4867 preserves the #4847 mechanical envelope but changes the electrical baseline to 79 rpm no-load, 0.10 A no-load, and 1.8 A/11 kg-cm extrapolated stall per motor. The 68.45 mm motor body, 4 x 12.5 mm D shaft, 17 mm face-hole spacing, bracket, three 3 mm tray spacers per side, hub cavity, four wheel screws, encoder-lead corridor, and 2 mm service covers are drawing-backed and validator-enforced. Printed pods are removable shrouds, not primary motor retention. Keep the shared Z=66 axle, print pods on the outer axle face, and block powered release until purchased-revision inspection, D-shaft/set-screw engagement, screw length, encoder, wheel retention, <=0.45 A-per-motor steady target-surface current, thermal, deterministic cutoff, and loaded floor tests pass.
- Each front-support pod uses two 608 bearings around a WDS 615-M6-8-65 shoulder bolt with a 65 mm-long, 8 mm shoulder and M6 thread, plus an M6 washer, prevailing-torque locknut, and stock goBILDA spacers/shim. Plastic locates the bearings; purchased metal provides positive retention. Require delivered-hardware fit, thread engagement, spacer squareness, controlled axial play without bearing preload, wheel alignment, locknut retention, and loaded skid-turn tests. Plastic threads or friction fits must never retain the axle.
- The safety MCU baseline is a non-wireless Raspberry Pi Pico 2 without headers, stacked above the motor-controller plate at (-65, 33.5). The 51 x 21 x 1 mm board and 52.3 x 21 x 3.8 mm STEP envelope follow Raspberry Pi reference RP-009061-CA-2; four 2.1 mm holes on 48.26 x 17.78 mm spacing use 6 mm M2 standoffs. The USB end faces robot-front into a shelf notch/cable corridor and the SWD end faces inward/rear. Preserve those service paths and the wired deterministic stop/watchdog interface; re-run board-hole, standoff, notch, fit, and corridor checks after moving either plate.
- The motor-controller baseline is the Cytron MDDS10 from Cytron's official 101.092 x 66.802 mm STEP model. Its four 3 mm holes use 95.25 x 60.96 mm spacing and 6 mm M3 standoffs; terminals face robot-front into a 24 x 44 x 16 mm service corridor, and 10 mm of cooling clearance stays below the Pico shelf. The locally notched 124 x 74 x 4 mm printed plate sits 5 mm above the tray on four metal M3 spacers. Preserve keyed polarity, fused/cutoff wiring, terminal strain relief, and airflow; release still requires the purchased-board revision plus polarity, terminal-bend, thermal, and loaded-motor tests.
- The BLF-1203AB cradle sits directly on the tray with four M3 tabs and four pads meeting the selected pack bottom; two 20 mm straps pass through independent transverse channels. Explicit 3.4 mm service scallops clear the front pair of continuous power-deck standoffs; preserve those independent metal load paths. The controller plate uses four tray anchors and four 5 mm metal spacers, then carries four MDDS10 standoffs and the separate four-standoff Pico shelf. Do not move either floor assembly without rechecking tray/handle clearance, screw paths, standoff endpoints, terminal access, cooling, and fit-envelope support.
- Front ToF boards belong at Y +/-16.5 behind the two apertures in the central black sensor bar; the lime windows at Y +/-50 are LED diffusers, not sensor covers. All four expression lights target Adafruit 5975 NeoPixel JST breakouts with exact 12.192 x 11.43 x 5.93 mm STEP envelopes, two M2 holes on 8.636 mm spacing, two 3-pin JST-SH ports, two 3 mm-OD spacers per board, and separate removable M2.5 carrier frames. The eye boards rotate 90 degrees so their plug paths miss the carrier screws and fixed yoke. Keep brightness software-limited and release blocked on delivered-part, cable-bend, diffuser-hotspot, camera-reflection, and 5 V branch tests.
- `docs/cad-component-coverage.md` is the BOM-to-CAD release matrix. Update it whenever a component envelope, mount, service path, or hardware-selection gate changes.
- Printed parts should be modular and serviceable: base tray, electronics deck, battery cradle, motor pods, bumper carrier, ToF sensor pods, camera/head bracket, and top shell.
- Use `preview_only()` in OpenSCAD for dummy hardware, LED/status markers, keepout markers, and other review-only solids so generated STLs stay print-focused.
- Include USB storage mounting as a serviceable later module only if USB SSD/flash storage becomes part of the build.
- Do not rely on printed plastic alone for safety-critical battery retention, E-stop mounting, axle support, motor retention, or carrying the loaded robot.

## Python CAD Setup

The macOS system Python found earlier was `3.9.6`, which is too old for current `build123d` expectations. Use a newer Homebrew Python and a venv for CAD work:

```bash
brew install python@3.12
/opt/homebrew/bin/python3.12 -m venv .venv-cad
source .venv-cad/bin/activate
python -m pip install --upgrade pip
python -m pip install -r cad/python/requirements.txt
```

Do not install CAD packages into Apple/system Python.

## Repo Hygiene

- Use `rg` / `rg --files` for searching.
- Use `apply_patch` for manual file edits.
- Do not edit generated CAD exports by hand.
- Do not commit or track `.venv-cad/`, `cad/exports/`, logs, captures, rosbags, or generated STL/STEP files unless the user explicitly asks for release artifacts.
- The working tree may already contain uncommitted user or agent changes. Do not revert unrelated changes.

## Near-Term Next Steps

Likely next useful work:

1. Print and record all 18 v2 qualification proofs in the exact intended PLA/PETG/TPU product lines. Blank evidence keeps the body at dry-assembly prototype status.
2. Close the purchase blockers in `docs/builder-release-v2.md`: current D36V50F6 availability (or the dual-footprint alternate) and delivered-revision confirmation for every modeled component. Do not improvise substitutions. (The R-ML24 blocker is retired by D048.)
3. Complete an electrical design review for the protected mobile Pi input, separate battery-near motor and accessory feeder fuses, production default-off relay driver, Pico-local physical reset, microphone VBUS cut/no-backfeed path, terminals, conductors, and crimp tooling. Fuse values remain unset until measured evidence supports them.
4. Convert `harness/harness-v2.json` revision H1 into a measured production harness: 28 routes including Pico-to-head PWM, exact connector/terminal selections, lengths, gauges, polarity labels, strain relief, and continuity/short-test records.
5. Bench-qualify the purchased Pi supply, D36 servo rail, relay/E-stop path, MDDS10, #4867 motors, D85MG head stacks with their bag arms in the capture cradles, charger inhibit, six NC bumper loops, and hardware mute before installing power in the body.
6. Run the commissioning plan with real firmware outputs and captured physical evidence. CAD PASS, host tests, a pretty book, and robot charm do not authorize powered motion; every required release gate must pass and be pinned to the exact hardware/software build.
